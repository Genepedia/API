; Raise request body limits so base64 image uploads from the profile media
; tab are not rejected with HTTP 413/400 before PHP runs.
post_max_size = 32M
upload_max_filesize = 32M
memory_limit = 256M
max_input_time = 120
max_execution_time = 120
